# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
from platform import node


class Solution:
    def __init__(self):
        self.temp = -1
        self.diff = 0
    def traverse(self, root: node):
        # nonlocal tmp
        # nonlocal diff
            
        # if node is None:
        #     return
            
        # self.traverse(node.right)
        # if tmp >= 0:
        #     diff = min(diff, tmp - node.val)
        # tmp = node.val
        # self.traverse(node.left)
        pass
        
    def minDiffInBST(self, root: node) -> int:
        pass